ansible --version
ansible all --list-hosts
cat /etc/ansible/hosts
ansible apacheweb -s -m shell -a 'sudo yum install -y lynx'
ansible apacheweb -s -m yum -a 'pkg=telnet state=installed update_cache=true'
